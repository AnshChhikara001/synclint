Verified 0 sections; 0 have drifted. 1 names code the change deleted.

README.md#humanize > Usage > Integer humanization  (src/humanize/number.py::apnumber, deleted)
    It names `apnumber`, which no longer exists: the change removed it from `src/humanize/number.py`, and nothing by that name appeared anywhere else.

    Flagged: the code it describes is gone, so there is nothing for a repair to describe; rewriting the section or removing it is its author's call

0 model calls, 0 tokens in, 0 out, $0.0000 spent.
