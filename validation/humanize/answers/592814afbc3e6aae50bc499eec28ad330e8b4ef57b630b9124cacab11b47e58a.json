{
  "model": "gpt-5.4-mini",
  "system": "You verify whether documentation still describes code accurately.\n\nYou are given one section of a project's documentation and one chunk of code it\ndescribes, as that chunk read before a change and as it reads after. Decide\nwhether the change has made any part of the section wrong.\n\nJudge only what the section actually claims. A section that never mentioned the\nthing that changed has not drifted. Neither has one that describes behaviour in\nlooser terms than the code states it. Say a section has drifted only when a\nreader following it would now be misled.\n\nIf it has drifted, explain in one sentence what is now wrong. If it has not,\nleave the explanation empty.",
  "user": "Documentation section README.md#humanize > Usage > Human-readable floating point numbers:\n\n```pycon\n>>> import humanize\n>>> humanize.fractional(1/3)\n'1/3'\n>>> humanize.fractional(1.5)\n'1 1/2'\n>>> humanize.fractional(0.3)\n'3/10'\n>>> humanize.fractional(0.333)\n'333/1000'\n>>> humanize.fractional(1)\n'1'\n```\n\nThe chunk src/humanize/number.py::fractional before the change:\n\ndef fractional(value: NumberOrString) -> str:\n    import math\n    try:\n        number = float(value)\n        if not math.isfinite(number):\n            return _format_not_finite(number)\n    except (TypeError, ValueError):\n        return str(value)\n    from fractions import Fraction\n    whole_number = int(number)\n    frac = Fraction(number - whole_number).limit_denominator(1000)\n    numerator = frac.numerator\n    denominator = frac.denominator\n    if denominator == 1:\n        return f'{whole_number + numerator:.0f}'\n    if not whole_number:\n        return f'{numerator:.0f}/{denominator:.0f}'\n    return f'{whole_number:.0f} {abs(numerator):.0f}/{denominator:.0f}'\n\nAnd after it:\n\ndef fractional(value: NumberOrString) -> str:\n    import math\n    try:\n        number = float(value)\n        if not math.isfinite(number):\n            return _format_not_finite(number)\n    except (TypeError, ValueError):\n        return str(value)\n    from fractions import Fraction\n    whole_number = int(number)\n    frac = Fraction(number - whole_number).limit_denominator(1000)\n    numerator = frac.numerator\n    denominator = frac.denominator\n    if denominator == 1:\n        return f'{whole_number + numerator:.0f}'\n    if not whole_number:\n        return f'{numerator:.0f}/{denominator:.0f}'\n    improper = Fraction(number).limit_denominator(1000)\n    return f'{improper.numerator:.0f}/{improper.denominator:.0f}'",
  "text": "{\"accurate\":false,\"explanation\":\"`fractional(1.5)` no longer returns a mixed number like `1 1/2`; for values with a whole-number part the function now returns an improper fraction such as `3/2`.\"}",
  "input_tokens": 671,
  "output_tokens": 151
}
